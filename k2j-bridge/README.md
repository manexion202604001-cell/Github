# K2J Bridge — 韓国→日本 商品開発支援プラットフォーム

韓国企業が日本市場向けに新商品を開発するための支援プラットフォームのサイトです。**サイトのUIは英語版のみ**です(旧日本語版UIはgit履歴に残っています)。
「日本でいま何が売れているか」のトレンド分析から、OEM商品企画・デザイン提案・マーケティングまでをワンストップで支援するプロダクトのコンセプトを、ランディングページ+操作できるデモダッシュボードとして実装しています。

既存の顧客台帳アプリと同じく **ビルド不要・静的HTMLのみ** の構成です。ブラウザで開くだけで動きます。

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | ランディングページ(サービス紹介・料金・FAQ) |
| `dashboard.html` | デモダッシュボード(5タブ・サンプルデータで動作) |
| `intro.html` | 紹介資料(英語・韓国語・日本語の3言語切替対応。利用者像/Before・After/使い方ガイドを掲載。印刷・PDF保存に最適化) |

## デモダッシュボードの内容

| タブ | 内容 |
|---|---|
| 📊 トレンド分析 | **国内主要ECモールをメインソース**としたカテゴリー別(美容・食品・生活雑貨)の急上昇ランキング、KPI、成長率グラフ、レビューAIインサイト |
| 💡 AI商品企画 | カテゴリー・価格帯・販路を選ぶと、OEM商品コンセプト+**商品イメージ画像(3案)**+規制チェック+工場マッチングを生成。**AI APIキーを設定すると実際にAIがリアルタイム生成**(未設定時はサンプル表示)。**画像生成APIキーも設定すると、商品イメージ画像はSVGモックアップではなく実際にAIが生成した画像**になる。**収益シミュレーター・商標事前確認・見積依頼文コピー・提案書の印刷/PDF保存**などメーカー企業の実務に直結する機能を搭載 |
| 🎨 デザイン提案 | 実在するデザイン・実商品の画像だけを全幅マソンリーで流し見できる**インスピレーションフィード**。検索窓+カテゴリチップ+無限スクロール。キー不要で動作し、実画像検索(BYOK)設定時はデザイン参考サイトの実画像が優先表示 |
| 🏭 工場・原価管理 | **OEM/ODM工場ディレクトリ**(検索・カテゴリ絞り込み・並び替え・RFQメール文コピー)+**原価プランナー**(製造原価・梱包・輸送・関税・手数料・広告費から着荷原価/総原価/粗利/損益分岐をリアルタイム試算)+**商品ポートフォリオ管理**(保存した商品の原価構造・月次利益を一覧管理、合計KPI表示、CSVダウンロード。localStorageに保存) |
| 🚀 マーケティング | 発売までの6ヶ月ロードマップ、販売チャネル適合度、ローンチKPIの目安 |

## AI APIによるAI商品企画(BYOK方式)

「AI商品企画」タブでは、ユーザー自身のAI APIキーを使って実際にAIが商品コンセプトを生成できます。

- キー入力欄は `⚙️ AI API設定` から開けます
- **APIキーはブラウザの `localStorage` にのみ保存され、AI APIの提供元へブラウザから直接送信されます。当サイトのサーバーには一切送信・保存されません**
- API利用料はキーの持ち主(あなた)のAPIアカウントに発生します
- 生成プロンプトには「実在するブランド名・商品名と重複・類似させない、完全にオリジナルな名称にする」制約を明示しており、既存商品名との衝突を避けます
- APIキー未設定時、または呼び出しに失敗した場合は自動的にサンプルデータへフォールバックします

⚠️ **セキュリティ上の注意**: これは個人利用・プロトタイピング向けの簡易実装(BYOK: Bring Your Own Key)です。共有PCや複数人が使う環境では使用しないでください。不特定多数が使うサービスとして展開する場合は、APIキーをサーバー側(Supabase Edge Functions や Vercel Serverless Functions など)で管理し、フロントエンドに直接キーを渡さない構成に変更することを強く推奨します。

## 商品イメージ画像のAI生成(BYOK方式)

「AI商品企画」タブの `⚙️ 商品イメージ生成設定` から、画像生成APIキーを設定すると、商品イメージ画像(3案)がSVGで描画したパッケージ形状のモックアップではなく、**AIが生成した実際の画像**になります。

- キーはAI企画生成用のキーとは別に、`localStorage`にのみ保存されます(当サイトのサーバーには送信・保存されません)
- 生成プロンプトには「実在するブランド名・ロゴを描かない」制約を明示しています
- 未設定時、または生成に失敗した場合は自動的にSVGモックアップにフォールバックします
- 画像生成には数十秒かかる場合があります。生成中はカードごとにローディング表示になります
- API利用料はキーの持ち主(あなた)のAPIアカウントに発生します

## メーカー企業の実務に直結する機能(AI商品企画タブ)

生成された提案には、以下の実務機能が付属します。

- **💹 収益シミュレーター**: 想定売価・原価率・月間販売目標・月間広告費を入力すると、粗利/個・月間粗利・広告費控除後利益・損益分岐販売数をリアルタイム試算(簡易計算・税抜)
- **⚖️ 商標の事前確認**: AIが生成したブランド名は完全オリジナルですが、実際に使用する前に[特許庁の商標検索システム](https://www.j-platpat.inpit.go.jp/)・Web検索で商標の重複有無を確認できるリンクを用意
- **📋 見積依頼文のコピー**: マッチしたOEM/ODM工場ごとに、商品仕様・想定価格・確認事項を含む日本語の見積依頼文をワンクリックでクリップボードにコピー(実際の工場担当者へのメールにそのまま貼り付け可能)
- **🖨 提案書の印刷/PDF保存**: 生成した提案をそのまま印刷、またはブラウザの「PDFに保存」機能で一枚のPDF資料として出力し、社内共有・決裁に活用可能

## デザイン提案:インスピレーションフィード

「デザイン提案」タブは、**実在するデザイン・実商品の画像だけを全幅のタイル(マソンリー)で流し見できるフィード**です。AI生成画像は使用しません(AI生成は「AI商品企画」タブの商品イメージのみ)。

- 上部の**検索窓**でキーワード検索、**カテゴリチップ**(おすすめ/コスメ/食品パッケージ/家電・ガジェット/ミニマル/ポップ・カラフル/高級感/ブランディング/配色)でワンタップ切替
- スクロールすると自動で追加読み込み(無限スクロール)。「もっと見る」ボタンでも読み込めます
- 各カードはクリックで出典元ページを新しいタブで開きます。出典ドメイン・作者・ライセンスをカード下に表示

### 画像ソース(優先順)

1. **実画像検索(任意・BYOK)**: `⚙️ 実画像検索の設定` でAPIキーと検索エンジンID(cx)を設定すると、デザイン参考サイトに絞った実画像検索結果がフィードに優先的に流れます(無料枠 1日100クエリ)
2. **CCライセンス画像検索**(キー不要・常時): 著作権フリー/クリエイティブ・コモンズの実写真
3. **写真共有サービスの公開フィード**(キー不要・常時): タグ一致の実写真(公式の公開フィード API を利用)

⚠️ いずれも外部サイトの無断スクレイピングではなく、公式に公開されているAPI・フィードを利用しています。画像は保存・再配布せず、その場で表示して出典元ページへリンクしています。

## 公開方法

このリポジトリをGitHub Pagesで公開している場合、そのまま以下のURLでアクセスできます。

```
https://<アカウント名>.github.io/<リポジトリ名>/k2j-bridge/
```

## 注意

- トレンドランキング・数値・工場情報はすべて**説明用のサンプルデータ**です(実データ連携は今後の実装)
- 商品企画は、AI APIキー未設定時は**サンプルの商品コンセプト**(VEILMER/HANMAEK/BREEZOという架空ブランド、実在商品とは無関係)を表示します
- 「AI商品企画」タブの商品イメージ画像は、画像生成APIキー未設定時はSVGで描画したパッケージ形状のモックアップ、設定時はAIが生成した画像です。いずれも実際の商品写真ではありません
- 「デザイン提案」タブのフィードは、公式に公開されているAPI・フィード経由で取得した実在画像のみを表示します。表示内容・品質は各ソースの検索結果に依存します
- 各外部サイトへのリンクは、キーワード検索結果ページへの実際のリンクです(特定の画像・作品の存在を保証するものではありません)
- 収益シミュレーター・原価プランナーは簡易計算です(税・送料・為替変動・返品率などは考慮していません)。実際の事業計画には専門家の確認を推奨します
- 「工場・原価管理」タブの工場情報はすべて説明用のサンプルデータです(実在の工場・連絡先ではありません)。保存した商品リストはブラウザのlocalStorageにのみ保存されます
- 商標事前確認リンクは検索の補助であり、商標登録の可否を保証するものではありません。実際の出願前には弁理士等の専門家にご確認ください
- 見積依頼文のコピー機能はテキスト生成のみです(サンプルの工場情報に実在の連絡先はありません)
- 外部ライブラリ・CDNに依存せず、HTML/CSS/JSのみで完結しています

## 今後の拡張候補

- ECモール実データ連携(ランキング・レビュー収集基盤、他モールへの拡張)
- AI API/画像検索APIキーをサーバー側で安全に管理する構成への移行(サーバーレス関数など)
- 外部サービス公式APIとの連携(法人向けAPIアクセス許可を取得できた場合、実際の投稿を直接表示)
- 見積依頼の送信・ステータス管理機能(工場とのやり取りをプラットフォーム内で完結)
- 収益シミュレーターへの為替レート・送料・関税の自動反映
- 韓国語UIの追加(i18n)
- Supabaseによるユーザー登録・保存機能(既存アプリと同じ構成で追加可能)
