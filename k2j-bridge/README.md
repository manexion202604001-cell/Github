# K2J Bridge — 韓国→日本 商品開発支援プラットフォーム

韓国企業が日本市場向けに新商品を開発するための支援プラットフォームのサイトです。
「日本でいま何が売れているか」のトレンド分析から、OEM商品企画・デザイン提案・マーケティングまでをワンストップで支援するプロダクトのコンセプトを、ランディングページ+操作できるデモダッシュボードとして実装しています。

既存の顧客台帳アプリと同じく **ビルド不要・静的HTMLのみ** の構成です。ブラウザで開くだけで動きます。

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | ランディングページ(サービス紹介・料金・FAQ) |
| `dashboard.html` | デモダッシュボード(4タブ・サンプルデータで動作) |

## デモダッシュボードの内容

| タブ | 内容 |
|---|---|
| 📊 トレンド分析 | **Qoo10(日本)をメインソース**としたカテゴリー別(美容・食品・生活雑貨)の急上昇ランキング、KPI、成長率グラフ、レビューAIインサイト |
| 💡 AI商品企画 | カテゴリー・価格帯・販路を選ぶと、OEM商品コンセプト+**商品イメージ画像(3案)**+規制チェック+工場マッチングを生成。**Claude APIキーを設定すると実際にAIがリアルタイム生成**(未設定時はサンプル表示) |
| 🎨 デザイン提案 | **AI商品企画で生成した商品、または自社商品情報を読み込ませる**と、Pinterest風マスonryグリッドで**実際の参考写真**(Openverse経由、出典・ライセンス表記付き)を表示。**Pinterest・Behance・Dribbble・Qoo10の実際の商品/作品**への外部リンク付き。業界共通の6デザイントレンドも実写真で確認可能 |
| 🚀 マーケティング | 発売までの6ヶ月ロードマップ、販売チャネル適合度、ローンチKPIの目安 |

## Claude APIによるAI商品企画(BYOK方式)

「AI商品企画」タブでは、ユーザー自身の Anthropic APIキーを使って実際に Claude が商品コンセプトを生成できます。

- キー入力欄は `⚙️ Claude API設定` から開けます
- **APIキーはブラウザの `localStorage` にのみ保存され、Anthropic社のAPIへブラウザから直接送信されます。当サイト(GitHub Pages)のサーバーには一切送信・保存されません**
- API利用料はキーの持ち主(あなた)のAnthropicアカウントに発生します
- 生成プロンプトには「実在するブランド名・商品名と重複・類似させない、完全にオリジナルな名称にする」制約を明示しており、既存商品名との衝突を避けます
- APIキー未設定時、または呼び出しに失敗した場合は自動的にサンプルデータへフォールバックします

⚠️ **セキュリティ上の注意**: これは個人利用・プロトタイピング向けの簡易実装(BYOK: Bring Your Own Key)です。共有PCや複数人が使う環境では使用しないでください。不特定多数が使うサービスとして展開する場合は、APIキーをサーバー側(Supabase Edge Functions や Vercel Serverless Functions など)で管理し、フロントエンドに直接キーを渡さない構成に変更することを強く推奨します。

## デザイン提案:Pinterest風の実写真リファレンス

「デザイン提案」タブは、Pinterest本家(jp.pinterest.com)のようなマスonry(タイル状)レイアウトで**実際の写真**を並べる参考資料ボードです。イラスト・モックアップではなく実写真での参考を意図しています。

以下いずれかの方法で商品を読み込ませると、その商品専用の参考写真ボードを生成できます。

- **🤖 AI商品企画で生成した商品を読み込む**: 「AI商品企画」タブで生成した商品(直近10件、ブラウザに保存)から選択
- **✍️ 自社商品を入力する**: 商品名・カテゴリー・特徴キーワードを直接入力

生成される内容:
- デザインの方向性3案それぞれについて、実際の参考写真をマスonryグリッドで表示(Claude APIキー設定時は方向性もAIが提案)
- 各写真には出典(撮影者)・ライセンスを表示し、クリックで出典元ページへ
- 各案から本家 **Pinterest** の検索結果、**Behance・Dribbble** の実例検索、**Qoo10** の類似実商品検索へワンクリックで遷移可能

### 画像の取得元について

Pinterestは外部サイトから画像を取得できる公開APIを提供していないため、参考写真は著作権フリー/クリエイティブ・コモンズライセンスの実写真を検索できる [Openverse](https://openverse.org)(APIキー不要・利用規約に準拠した公開API)から取得しています。Openverseに十分な検索結果がないニッチなキーワードの場合、画像が少ない・表示されないことがありますが、その場合も「Pinterestで見る」ボタンから本家Pinterestでの検索は常に可能です。

## 公開方法

このリポジトリをGitHub Pagesで公開している場合、そのまま以下のURLでアクセスできます。

```
https://<アカウント名>.github.io/<リポジトリ名>/k2j-bridge/
```

## 注意

- トレンドランキング・数値・工場情報はすべて**説明用のサンプルデータ**です(実データ連携は今後の実装)
- 商品企画は、Claude APIキー未設定時は**サンプルの商品コンセプト**(VEILMER/HANMAEK/BREEZOという架空ブランド、実在商品とは無関係)を表示します
- 「AI商品企画」タブの商品イメージ画像はSVGで描画したモックアップです(実際の商品写真ではありません)。実サービスでは写真品質の画像生成モデルへの置き換えを想定
- 「デザイン提案」タブの参考写真はOpenverse経由の実写真ですが、Pinterest本体が実際にインデックスしている画像そのものではありません(Pinterestに公開APIがないため)。あくまで同じキーワードで検索した実写真の参考資料です
- Pinterest/Behance/Dribbble/Qoo10への外部リンクは、キーワード検索結果ページへの実際のリンクです(特定の画像・作品の存在を保証するものではありません)
- 外部ライブラリ・CDNに依存せず、HTML/CSS/JSのみで完結しています

## 今後の拡張候補

- Qoo10実データ連携(ランキング・レビュー収集基盤、楽天・Amazonへの拡張)
- Claude APIキーをサーバー側で安全に管理する構成への移行(Supabase Edge Functions / Vercel Serverless Functions)
- 商品イメージ画像のAI画像生成モデル連携(SVGモックを写真品質の画像に置き換え)
- Pinterest公式APIとの連携(法人向けAPIアクセス許可を取得できた場合、実際のPinterestピンを直接表示)
- 韓国語UIの追加(i18n)
- Supabaseによるユーザー登録・保存機能(既存アプリと同じ構成で追加可能)
